streamlit==1.45.1
pandas==2.2.3
google-auth-oauthlib==1.2.0  # Google API 인증에 필요 (직접 명시되지 않았지만 종속성으로 필요할 수 있음)
google-api-python-client==2.128.0 # Google Sheet API와 상호작용에 필요
google-generativeai==0.8.0 # Gemini API 호출에 필요
# toml은 보통 직접적인 파이썬 패키지로 명시하기보다 secrets.toml 파일 자체의 포맷을 의미합니다.
# 하지만 경우에 따라 'toml' 패키지가 명시적으로 필요할 수 있으므로, 만약을 위해 포함합니다.
# 일반적으로 Streamlit이 secrets.toml을 내부적으로 처리하므로 명시적인 필요는 적습니다.
toml==0.10.2 
numpy==1.24.3
python-dateutil==2.8.2
requests==2.31.0
openpyxl